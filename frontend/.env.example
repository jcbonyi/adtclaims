# Optional — overrides production default (`/api` via `.env.production`).
# Local `vite` still uses `/api` (proxied to Express) unless you set this.
#
# Absolute example if API is elsewhere:
# VITE_API_BASE_URL=https://your-api.up.railway.app/api
