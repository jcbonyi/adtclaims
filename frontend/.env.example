# Optional — production / preview builds only.
# Full axios base URL (must include `/api`). Example:
# VITE_API_BASE_URL=https://your-api.up.railway.app/api
#
# Leave unset for local dev; `vite.config.js` proxies /api to the backend.
